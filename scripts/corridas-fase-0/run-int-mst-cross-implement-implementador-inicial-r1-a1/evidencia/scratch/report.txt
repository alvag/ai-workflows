## Reporte

1. **Encargo:** actuar como implementador cross-model: aplicar un **work order congelado**, ejecutar su prueba autorizada y no hacer commit ni escribir fuera de `working_dir`. La sección por sí sola define el despacho, no un cambio de código concreto (`skills/cross-implement/reference.md:68-151`).

2. **Entrada necesaria:** `prompt.txt` con GOAL, SPEC/work order, rutas, restricciones, no-objetivos, prueba y formato de salida; además de `working_dir` y `proof_cmd`. Para una corrección: `session.txt` y `fix-rN.txt` (`skills/cross-implement/SKILL.md:112-155`).

3. **Salida:** un diff sin commit dentro de `working_dir` y `report.txt` con archivos modificados, prueba y exit code, desviaciones y `STATUS: done`. El despachador cosecha además la sesión, transcript, errores y manifest (`skills/cross-implement/reference.md:200-214`).

## Key Learnings:

1. Las vías W-B/W-C transportan un contrato congelado; no sustituyen el work order concreto.