1. **Encargo:** despachar una revisión adversarial de un artefacto SDD mediante un revisor independiente y read-only, persistiendo el prompt congelado y enviándolo por `stdin`; luego cosechar su veredicto sin permitir escrituras del revisor.

2. **Entrada necesaria:** `artifact_type`, `artifact_path`, `working_dir`, contenido y contexto del artefacto, criterios de aceptación, complejidad y el prompt exacto de revisión.

3. **Salida esperada:** hallazgos estructurados y veredicto `APPROVED`, `REVISE` o `UNAVAILABLE`; archivos trazables en `cross-review/` y el registro consolidado `review-log.md`. El artefacto permanecería intacto salvo cambios posteriores aplicados por Claude.

No ejecuté el encargo ni escribí archivos.

## Key Learnings:

1. El prompt del revisor debe persistirse y enviarse por `stdin`, nunca interpolarse en shell.
2. La revisión falla cerrada si no puede garantizarse el aislamiento read-only.

